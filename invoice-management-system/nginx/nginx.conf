# nginx/nginx.conf
events {}

http {
  server {
    listen 8081;

    location /api/ {
      proxy_pass http://backend:8881;
    }

    location / {
      proxy_pass http://frontend:3000;
    }
  }
}
